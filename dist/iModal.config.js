if (typeof define === 'function' && !!define.$config) {
    define.$config({
        sites: {/* lib: "javascript/lib/" */},
        paths: {/* config: "lib!iModal.config" */},
        charset: "utf-8"
    });
}